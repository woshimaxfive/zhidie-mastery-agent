# 协作规范

## 基本流程

1. 每项工作先建立 Issue，写清目标、边界和验收标准。
2. 从最新 `main` 创建短生命周期分支。
3. 完成定向测试后发起 Pull Request。
4. 至少由一名未参与该修改的贡献者审查。
5. 自动检查和人工验收通过后合并；禁止直接向 `main` 推送。

## 分支命名

```text
feat/learning-workspace
feat/mastery-engine
fix/transfer-validation
docs/api-contract
test/core-learning-loop
```

## 模块边界

- 前端：页面、交互、可访问性和前端测试。
- 后端与 Agent：API、领域规则、持久化、Trace 和后端测试。
- 公共契约：`docs/` 下的需求、设计和 API 文件应与实现保持同步。

模块边界用于减少耦合。跨模块修改需要在 Pull Request 描述中说明影响。

## Pull Request 要求

PR 描述至少包含：

- 解决的问题；
- 明确未解决的内容；
- 用户可见变化；
- 测试方法和结果；
- 是否改变 API、状态枚举、数据库或合规边界；
- 涉及界面时附截图或录屏。

一个 PR 尽量只解决一个问题。不要把大范围格式化、依赖升级和业务功能混在一起。

## 契约变更

以下修改必须同步更新文档，并在 Pull Request 中明确标注：

- API 请求或响应字段；
- 会话阶段、掌握状态或证据等级；
- 掌握判定规则；
- 数据库迁移；
- 新增外部服务、依赖、数据源或模型调用。

## 完成标准

功能只有在满足以下条件时才算完成：

- 符合对应 Issue 的验收标准；
- 正常路径和至少一个失败路径经过验证；
- 相关测试通过；
- 没有提交密钥、真实数据或非公开材料；
- 用户可见能力与 README 及产品文档一致。

## 文件与安全

- 所有代码和文本文件使用 UTF-8 无 BOM。
- 本地配置放入 `.env`，仅提交脱敏的 `.env.example`。
- SQLite 数据库和真实学习记录不得提交。
- 未获授权的第三方材料和个人资料不得提交到仓库。
- 不强制覆盖他人提交，不使用 `git push --force` 修改共享分支历史。
